<UserControl x:Class="PragmaticAnalyzer.MVVM.Views.Main.CommunicationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Верхний блок: статус, управление и настройки модели -->
        <Border Grid.Row="0"
                Margin="5"
                Padding="8"
                BorderBrush="Black"
                BorderThickness="1"
                CornerRadius="6"
                Background="#F5F5F5">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Верхняя строка: статус и кнопки управления моделью -->
                <DockPanel Grid.Row="0"
                           LastChildFill="True">
                    <ToggleButton x:Name="ModelSettingsToggleButton"
                          Content="⚙"
                          Width="36"
                          Height="32"
                          Margin="3,0"
                          FontSize="16"
                          FontWeight="Bold"
                          ToolTip="Настройки модели" /> 

                    <StackPanel DockPanel.Dock="Right"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">

                        <Button Content="Запустить модель"
                                Style="{StaticResource OutlinedGrayButtonStyle}"
                                Margin="3,0"
                                Command="{Binding StartModelCommand}" />

                        <Button Content="Остановить модель"
                                Style="{StaticResource OutlinedGrayButtonStyle}"
                                Margin="3,0"
                                Command="{Binding StopModelCommand}" />

                        <Button Content="Проверить подключение"
                                Style="{StaticResource OutlinedGrayButtonStyle}"
                                Margin="3,0"
                                Command="{Binding CheckServerCommand}" />
                    </StackPanel>

                    <TextBlock Text="{Binding ServerStatusText}"
                               VerticalAlignment="Center"
                               FontWeight="Bold"
                               Margin="0,0,10,0" />
                </DockPanel>

                <!-- Нижняя строка: настройки модели -->
                <Grid Grid.Row="1"
                      Margin="0,10,0,0">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed" />

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=ModelSettingsToggleButton}"
                             Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Grid.Column="0"
                               Text="koboldcpp.exe:"
                               VerticalAlignment="Center"
                               Margin="0,3" />

                    <TextBox Grid.Row="0"
                             Grid.Column="1"
                             Text="{Binding KoboldCppPath, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RoundedTextBoxStyle}"
                             Margin="3" />

                    <Button Grid.Row="0"
                            Grid.Column="2"
                            Content="Выбрать"
                            Style="{StaticResource OutlinedGrayButtonStyle}"
                            Command="{Binding SelectKoboldCppPathCommand}" />

                    <TextBlock Grid.Row="1"
                               Grid.Column="0"
                               Text="GGUF модель:"
                               VerticalAlignment="Center"
                               Margin="0,3" />

                    <TextBox Grid.Row="1"
                             Grid.Column="1"
                             Text="{Binding ModelPath, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RoundedTextBoxStyle}"
                             Margin="3" />

                    <Button Grid.Row="1"
                            Grid.Column="2"
                            Content="Выбрать"
                            Style="{StaticResource OutlinedGrayButtonStyle}"
                            Command="{Binding SelectModelPathCommand}" />

                    <TextBlock Grid.Row="2"
                               Grid.Column="0"
                               Text="Порт:"
                               VerticalAlignment="Center"
                               Margin="0,3" />

                    <TextBox Grid.Row="2"
                             Grid.Column="1"
                             Text="{Binding TranslatorPort, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RoundedTextBoxStyle}"
                             Margin="3" />

                    <TextBlock Grid.Row="3"
           Grid.Column="0"
           Text="Контекст:"
           VerticalAlignment="Center"
           Margin="0,3" />

                    <TextBox Grid.Row="3"
         Grid.Column="1"
         Text="{Binding ContextSize, UpdateSourceTrigger=PropertyChanged}"
         Style="{StaticResource RoundedTextBoxStyle}"
         Margin="3" />

                    <TextBlock Grid.Row="4"
           Grid.Column="0"
           Text="Max tokens:"
           VerticalAlignment="Center"
           Margin="0,3" />

                    <TextBox Grid.Row="4"
         Grid.Column="1"
         Text="{Binding MaxTokens, UpdateSourceTrigger=PropertyChanged}"
         Style="{StaticResource RoundedTextBoxStyle}"
         Margin="3" />

                    <TextBlock Grid.Row="5"
           Grid.Column="0"
           Text="Temperature:"
           VerticalAlignment="Center"
           Margin="0,3" />

                    <TextBox Grid.Row="5"
         Grid.Column="1"
         Text="{Binding Temperature, UpdateSourceTrigger=PropertyChanged}"
         Style="{StaticResource RoundedTextBoxStyle}"
         Margin="3" />

                    <TextBlock Grid.Row="6"
           Grid.Column="0"
           Text="Top P:"
           VerticalAlignment="Center"
           Margin="0,3" />

                    <TextBox Grid.Row="6"
         Grid.Column="1"
         Text="{Binding TopP, UpdateSourceTrigger=PropertyChanged}"
         Style="{StaticResource RoundedTextBoxStyle}"
         Margin="3" />

                    <TextBlock Grid.Row="7"
           Grid.Column="0"
           Text="Repeat penalty:"
           VerticalAlignment="Center"
           Margin="0,3" />

                    <TextBox Grid.Row="7"
         Grid.Column="1"
         Text="{Binding RepeatPenalty, UpdateSourceTrigger=PropertyChanged}"
         Style="{StaticResource RoundedTextBoxStyle}"
         Margin="3" />
                    <TextBlock Grid.Row="8"
           Grid.Column="0"
           Text="История:"
           VerticalAlignment="Center"
           Margin="0,3" />

                    <TextBox Grid.Row="8"
         Grid.Column="1"
         Text="{Binding HistoryMessagesCount, UpdateSourceTrigger=PropertyChanged}"
         Style="{StaticResource RoundedTextBoxStyle}"
         Margin="3" />

                    <TextBlock Grid.Row="9"
           Grid.Column="0"
           Text="System prompt:"
           VerticalAlignment="Top"
           Margin="0,8,0,3" />

                    <TextBox Grid.Row="9"
         Grid.Column="1"
         Text="{Binding SystemPrompt, UpdateSourceTrigger=PropertyChanged}"
         Style="{StaticResource RoundedTextBoxStyle}"
         Height="120"
         AcceptsReturn="True"
         TextWrapping="Wrap"
         VerticalScrollBarVisibility="Auto"
         Margin="3" />

                    <Button Grid.Row="9"
        Grid.Column="2"
        Content="Сохранить настройки"
        Style="{StaticResource OutlinedGrayButtonStyle}"
        Command="{Binding SaveTranslatorConfigCommand}" />
                </Grid>
            </Grid>
        </Border>

        <!-- Список сообщений -->
        <ScrollViewer x:Name="MessagesScroll"
                      Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="5"
                      ScrollChanged="MessagesScroll_ScrollChanged">

            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="MessageBorder"
                                CornerRadius="12"
                                Padding="12,8"
                                Margin="8,4"
                                MaxWidth="600">

                            <StackPanel>
                                <TextBox x:Name="MessageTextBox"
                                             Text="{Binding Text, Mode=OneWay}"
                                             TextWrapping="Wrap"
                                             IsReadOnly="True"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             FontSize="16"
                                             FontFamily="Consolas"
                                             FontWeight="Bold"
                                             Padding="0"
                                             Cursor="IBeam" />

                                <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm'}"
                                           Foreground="#888"
                                           FontSize="10"
                                           HorizontalAlignment="Right"
                                           Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>

                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Sender}" Value="User">
                                <Setter TargetName="MessageBorder"
                                        Property="Background"
                                        Value="#4FC3F7" />
                                <Setter TargetName="MessageBorder"
                                        Property="HorizontalAlignment"
                                        Value="Right" />
                                <Setter TargetName="MessageBorder"
                                        Property="Margin"
                                        Value="60,4,8,4" />
                                <Setter TargetName="MessageBorder"
                                        Property="CornerRadius"
                                        Value="12,12,4,12" />
                            </DataTrigger>

                            <DataTrigger Binding="{Binding Sender}" Value="Assistant">
                                <Setter TargetName="MessageBorder"
                                        Property="Background"
                                        Value="#505050" />
                                <Setter TargetName="MessageBorder"
                                        Property="HorizontalAlignment"
                                        Value="Left" />
                                <Setter TargetName="MessageBorder"
                                        Property="Margin"
                                        Value="8,4,60,4" />
                                <Setter TargetName="MessageBorder"
                                        Property="CornerRadius"
                                        Value="12,12,12,4" />
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Индикатор отправки сообщения -->
        <ProgressBar Grid.Row="1"
                     IsIndeterminate="{Binding IsSending}"
                     Visibility="Visible"
                     VerticalAlignment="Top"
                     Height="4"
                     Margin="5" />

        <!-- Нижняя панель ввода -->
        <Grid Grid.Row="2"
              Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBox x:Name="InputBox"
                     Grid.Column="0"
                     Grid.Row="0"
                     Grid.RowSpan="2"
                     Height="90"
                     AcceptsReturn="False"
                     VerticalContentAlignment="Center"
                     Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource RoundedTextBoxStyle}"
                     KeyDown="InputBox_KeyDown" />

            <Button Grid.Column="1"
                    Grid.Row="0"
                    Content="➤"
                    Command="{Binding SendCommand}"
                    Style="{StaticResource OutlinedGrayButtonStyle}" />

            <Button Grid.Column="1"
                    Grid.Row="1"
                    Content="Очистить"
                    Command="{Binding ClearCommand}"
                    Style="{StaticResource OutlinedGrayButtonStyle}" />
        </Grid>
    </Grid>
</UserControl>
