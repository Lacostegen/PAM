<UserControl x:Class="PragmaticAnalyzer.MVVM.Views.Main.DatabaseSearchDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cc="clr-namespace:PragmaticAnalyzer.CastomControls"
             xmlns:vm="clr-namespace:PragmaticAnalyzer.MVVM.ViewModel.Main"
             d:DataContext="{d:DesignInstance Type=vm:DatabaseSearchDetailsViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="640"
             d:DesignWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Назад"
                    Style="{StaticResource OutlinedGrayButtonStyle}"
                    Command="{Binding BackCommand}" />

            <TextBlock Grid.Column="1"
                       Text="{Binding Result.Title}"
                       FontSize="24"
                       FontWeight="Bold"
                       FontFamily="Consolas"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center" />
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="{Binding Result.DatabaseName}"
                   Style="{StaticResource MainTextBlockStyle}"
                   FontWeight="Bold"
                   TextWrapping="Wrap"
                   Margin="10,0,10,5" />

        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto">
            <WrapPanel Margin="5">
                <ItemsControl ItemsSource="{Binding Result.Fields}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <cc:LabeledValueBox LabelText="{Binding Label}"
                                                ValueText="{Binding Value}"
                                                HighlightedValueSegments="{Binding HighlightedValueSegments}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
